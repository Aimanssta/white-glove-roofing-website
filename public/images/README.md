Place the homepage background image here.

Save the image you attached in the conversation as `homepage-roof.jpg` at this path:

`public/images/homepage-roof.jpg`

After placing the file, start the dev server (e.g., `npm run dev`) and open the site — the Hero background will use this image.